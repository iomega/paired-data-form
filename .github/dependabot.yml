version: 2
updates:
 - package-ecosystem: npm
   directory: api
   schedule:
     interval: 'monthly'
   open-pull-requests-limit: 0  # Disable non-security updates
 - package-ecosystem: npm
   directory: app
   schedule:
     interval: 'monthly'
   open-pull-requests-limit: 0  # Disable non-security updates
