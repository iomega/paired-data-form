import * as React from "react";

export function CloneProject() {
    return (
        <div>
            <h1>Clone page for submitting a new project, prefilled with existing project</h1>
        </div>
    );
}